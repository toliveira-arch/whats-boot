# =============================================================================
# whats-boot — Variáveis de ambiente (infraestrutura)
# Copie para `.env`:  cp .env.example .env
# =============================================================================

# ----- Ambiente -----
NODE_ENV=development
TZ=America/Sao_Paulo

# ----- API (Express) -----
API_PORT=3333
API_HOST=0.0.0.0
# Origens permitidas no CORS (separadas por vírgula)
CORS_ORIGINS=http://localhost:5173,http://localhost:8080

# ----- PostgreSQL -----
POSTGRES_USER=whatsboot
POSTGRES_PASSWORD=whatsboot
POSTGRES_DB=whatsboot
POSTGRES_PORT=5432
# Valores para DEV LOCAL (API/worker/prisma rodando na máquina, com Postgres e
# Redis expostos pelo docker compose em localhost). Dentro do Docker, o próprio
# docker-compose injeta os hosts "postgres"/"redis" — não precisa alterar aqui.
DATABASE_URL=postgresql://whatsboot:whatsboot@localhost:5432/whatsboot?schema=public

# ----- Redis -----
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_URL=redis://localhost:6379

# ----- Autenticação (placeholders — ETAPA 4) -----
JWT_ACCESS_SECRET=troque-esta-chave-de-access
JWT_REFRESH_SECRET=troque-esta-chave-de-refresh
JWT_ACCESS_TTL=15m
JWT_REFRESH_TTL=7d

# ----- Rate limit -----
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX=120

# ----- Frontend (Vite) -----
# Estas precisam do prefixo VITE_ para chegarem ao browser
VITE_API_URL=http://localhost:3333
VITE_SOCKET_URL=http://localhost:3333

# --- WhatsApp Cloud API (Meta / WABA oficial) -------------------------------
# Só necessário para canais oficiais. Canais Evolution/Baileys funcionam sem.
# VERIFY_TOKEN: string que voce escolhe e repete no painel da Meta ao cadastrar
# o webhook (Configuracao > Webhooks > Verificar token).
WHATSAPP_CLOUD_VERIFY_TOKEN=
# APP_SECRET: "Chave secreta do app" em Configuracoes do app > Basico.
# Sem ela, em producao o webhook recusa tudo (a URL e publica).
WHATSAPP_CLOUD_APP_SECRET=
# Versao da Graph API usada nos envios.
WHATSAPP_CLOUD_API_VERSION=v25.0
